TeacherSubject::all()
TeacherSubject::with('grade')->find(1)
TeacherSubject::with('studentGrade')->find(1)
TeacherSubject::with('studentGrade.student')->find(1)
TeacherSubject::with('studentGrade.student')->find(1)
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject->studentGrade->student;
$teacherSubject = TeacherSubject::with('studentGrade.student')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with('studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade->exam;
$teacherSubject = TeacherSubject::with([
                            'studentGrade.student',
                            'studentGrade.student.exam' => function($query) use ($get){
                                $query->where('category', $get('category'));
                                $query->first();
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
                            'studentGrade.student',
                            'studentGrade.student.exam' => function($query) use ($get){
                                $query->where('category','middle');
                                $query->first();
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
                                $query->where('category','middle');
                                $query->first();
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
                                $query->where('category','middle');
                                
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
                                $query->where('category','middle');
                                
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
                                
                                
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
                                $query->where('category','middle');
                                
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
                                $query->where('category','middle');
                                
                            },
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
    $query->first();
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
    $query->first();
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
    $query->first();
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
    $query->first();
},
])->find(1);
$teacherSubject->studentGrade->student;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
    $query->first();
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
    $query->first();
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.examEvaluation' => function($query){
	$query->where('category','middle');
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student',
'studentGrade.student.exam' => function($query){
	$query->where('category','middle');
},
])->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([

'studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with([
'studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with(
'studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with(
'studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
$teacherSubject = TeacherSubject::with(
'studentGrade.student.exam')->find(1);
$teacherSubject->studentGrade;
