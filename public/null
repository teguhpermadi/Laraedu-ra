TeacherSubject::with('competencies', 'grade.studentGrade')->mySubject()->get()
User::find(4)
User::find(3)
User::find(3)->userable

Teacher::with('teacherSubject.subject', 'teacherSubject.competencies')->find(2)

Teacher::with('teacherSubject.subject','teacherSubject.grade', 'teacherSubject.competencies')->find(2)
TeacherSubject::with('competencies')->get()
TeacherSubject::with('competencies')->where('teacher_id',2)->get()
TeacherSubject::with('competencies')->find(30)
