$curriculum = TeacherSubject::with('gradeCurriculum')->find(1);
$curriculum = TeacherSubject::with('competencies')->find(1);
$curriculum = TeacherSubject::with('competencies.studentCompetency')->find(1);
User::all()
